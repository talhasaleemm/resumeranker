# =========================================================
# requirements.txt — ResumeRanker
# Pinned to major versions for reproducibility
# =========================================================

# --- Web Framework ---
fastapi==0.139.0
uvicorn[standard]==0.32.0
python-multipart==0.0.31

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Configuration ---
pydantic-settings==2.6.1
pydantic==2.13.4
python-dotenv==1.2.2

# --- NLP / Resume Parsing ---
spacy==3.8.14
# PDF parsing
pdfplumber==0.11.9
PyMuPDF==1.24.14
# DOCX parsing
python-docx==1.1.2

# --- Matching Algorithms ---
scikit-learn==1.5.2
rank-bm25==0.2.2
numpy==2.1.3

# --- CLI (required by spaCy CLI) ---
click==8.4.2

# --- Rate Limiting ---
slowapi==0.1.10

# --- File Validation ---
python-magic-bin==0.4.14; sys_platform == 'win32'
python-magic==0.4.27; sys_platform != 'win32'

# --- Testing ---
pytest==9.0.3
pytest-asyncio==1.4.0
httpx==0.28.0

# --- Utilities ---
aiofiles==24.1.0

# --- Task Queue ---
celery[redis]==5.4.0
redis==5.2.1

# --- Vulnerability Patches (Transitive Overrides) ---
pdfminer.six==20251230
starlette==1.3.1

# --- OCR Fallback ---
pytesseract==0.3.10
pdf2image==1.17.0
Pillow==10.4.0

# --- Authentication ---
argon2-cffi==23.1.0
passlib[argon2]==1.7.4
PyJWT==2.8.0
